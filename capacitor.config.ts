import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ras.impostify',
  appName: 'Impostify',
  webDir: 'dist/impostor-frontend/browser'
};

export default config;
